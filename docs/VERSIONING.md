# Versioning

## Software and repository releases

Use semantic versioning for the public package and repository release.

Initial release tag: `v0.1.0-preprint`

## Research checkpoints

Data coverage, label status, model specifications, and published claims are frozen by checkpoint. A re-audit that changes those elements creates a new version and a new release. Tags and release assets are never reused or silently overwritten.
