# Results

`model_outputs/` contains frozen predictive baselines used by `scripts/verify_numeric.py`.

Do not treat regenerated outputs as replacements for frozen baselines unless a new release explicitly updates them.
